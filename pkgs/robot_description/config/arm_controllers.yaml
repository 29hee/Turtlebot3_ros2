controller_manager:
  ros__parameters:
    update_rate: 1000       #컨트롤러 제어 루프 주기(100Hz)

    joint_state_broadcaster:
      type: "joint_state_broadcaster/JointStateBroadcaster"

    effort_controller:
      type: "effort_controllers/JointGroupEffortController"
      # joints:
      #   - joint1
      #   - joint2
      #   - joint3
      #   - joint4
      #   - joint5
      #   - joint6

effort_controller:
  ros__parameters:
      joints:
        - arm_joint
      command_interfaces:
        - effort
      state_interfaces:
        - position
        - velocity

